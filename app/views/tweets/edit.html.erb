<div class="contents row">
  <div class="container">
    <h3>編集する</h3>
    <%= form_with(model: @tweet, local: true) do |form| %>
      <%= form.text_field :tilte, placeholder: "Title" %>
      <%= form.text_field :image, placeholder: "Image Url" %>
      <%= form.text_area :explanatory_text, placeholder: "explanatory_text", rows: "10" %>
    <% end %>
  </div>
</div>